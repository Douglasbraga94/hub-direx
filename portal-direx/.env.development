# ========================================
# PORTAL DIREX - DESENVOLVIMENTO LOCAL
# ========================================

DB_HOST=localhost
DB_NAME=db_portal_direx
DB_USER=seu_usuario_dev
DB_PASSWORD=sua_senha_dev
DB_PORT=3306

# Backend
BACKEND_PORT=8080
SPRING_PROFILE=dev

# JWT - Chave simples para dev (NUNCA use em produção!)
JWT_SECRET=dev_secret_key_not_for_production_use_only
JWT_EXPIRATION=86400000

# CORS - Permitir localhost para desenvolvimento
CORS_ORIGINS=http://localhost:4200,http://localhost:80

# Frontend
FRONTEND_PORT=4200
API_URL=http://localhost:8080/api
